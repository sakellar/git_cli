from setuptools import setup

setup(
   name='git_cli',
   version='1.0',
   description='A useful module for python cli commands',
   author='Angelos Sakellaropoulos',
   author_email='angsakel@gmail.com',
   python_requires='==2.7',
   install_requires=['mock'],
)
